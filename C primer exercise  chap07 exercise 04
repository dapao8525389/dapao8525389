//Using if else statements, write a program that reads input up to #, replaces
//each period with an exclamation mark, replaces each exclamation mark initially
//present with two exclamation marks, and reports at the end the number of
//substitutions it has made


#include <stdio.h>
#include <ctype.h>
#include <stdbool.h>
    int main()
    {
    int count=0;
    char ch;
    printf("Please enter sth:\n");
        while ((ch = getchar()) != '#')
        {
            if (ch == '.')
                {printf("!");
                count++;}
            else if (ch == '!')
                {printf("!!");
                count++;}
            else
                printf("%c", ch);
        }
    printf("this program has made %d times substitutions", count);
        return 0;

    printf("subs %d times", count);
    return 0;
    }

